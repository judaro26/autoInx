{
  "name": "e-commerce-netlify-app",
  "version": "1.0.0",
  "description": "Dependencies for the Netlify function (send-email.js) to run successfully.",
  "main": "index.html",
  "scripts": {
    "start": "echo 'This is a static site/serverless function project. Run directly with Netlify CLI if needed.'"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
