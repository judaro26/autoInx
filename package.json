{
  "name": "autoinx-ecommerce",
  "version": "1.0.0",
  "description": "AutoInx e-commerce platform with serverless functions",
  "main": "index.html",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'Static site with serverless functions'",
    "build:css": "npx tailwindcss -i ./css/input.css -o ./css/tailwind.min.css --minify",
    "watch:css": "npx tailwindcss -i ./css/input.css -o ./css/tailwind.min.css --watch",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "axios": "^1.6.2",
      "@anthropic-ai/sdk": "^0.52.0",
    "firebase-admin": "^11.11.0",
    "stripe": "^14.0.0",
    "nodemailer": "^6.9.7",
    "telnyx": "^2.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0",
    "tailwindcss": "^3.4.17"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
