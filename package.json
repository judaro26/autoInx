{
  "name": "Autoinx Ecommerce",
  "version": "1.0.0",
  "description": "Lightweight dependencies for autoInx admin functions including API-based PDF generation.",
  "main": "index.html",
  "scripts": {
    "start": "echo 'This is a static site/serverless function project. Run directly with Netlify CLI if needed.'",
    "build": "netlify deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "firebase-admin": "^11.11.0",
    "stripe": "^14.0.0",
    "nodemailer": "^6.9.7",
    "telnyx": "^2.0.0" 
  }
}
